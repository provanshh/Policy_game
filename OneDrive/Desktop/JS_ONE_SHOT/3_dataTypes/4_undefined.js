/*  undefined : 
        - if a variable is declared but the value is not assigned, 
            then the value of that variable will be undefined

        - and data type is also undefined
*/

let age;
console.log(age)
console.log(typeof age)
